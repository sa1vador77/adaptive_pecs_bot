# 🧩 Adaptive PECS Bot

Прототип адаптивного мобильного коммуникатора для детей с расстройством аутистического спектра (РАС). Реализован в виде Telegram-бота на Python.

Проект разработан в рамках курсовой работы.

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![Aiogram](https://img.shields.io/badge/Aiogram-3.x-blue)
![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0-red)

## 💡 Описание идеи

Бот заменяет физические карточки PECS (Picture Exchange Communication System). Основная особенность — **адаптивный алгоритм ранжирования потребностей**.

Система не просто считает клики («что чаще нажал, то и показываем»), а использует гибридную модель весов:

1. **Базовый приоритет:** Жизненно важные потребности (вода, туалет, боль) всегда имеют высокий вес, даже при редком использовании.
2. **Частота использования:** Логарифмический рост веса позволяет любимым предметам (игрушкам) подниматься выше, но не вытеснять критические потребности.

## 🛠 Технологический стек

* **Язык:** Python 3.13
* **Фреймворк бота:** `aiogram 3.x` (асинхронный, современные паттерны Routers/FSM)
* **База данных:** `SQLite` + `SQLAlchemy` (ORM, асинхронный драйвер `aiosqlite`)
* **Логирование:** `loguru` (структурированные логи)
* **Конфигурация:** `pydantic-settings`

## 🚀 Установка и запуск

1. **Клонируйте репозиторий:**

   ```bash
   git clone https://github.com/your-username/pecs-bot.git
   cd pecs-bot
   ```
2. **Создайте виртуальное окружение:**

   ```bash
   python -m venv venv
   source venv/bin/activate  # для Linux/macOS
   # venv\Scripts\activate   # для Windows
   ```
3. **Установите зависимости:**

   ```bash
   pip install aiogram sqlalchemy aiosqlite pydantic-settings loguru
   ```
4. **Настройте окружение:**
   Создайте файл `.env` и добавьте токен бота:

   ```env
   BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
   ```
5. **Запустите бота:**

   ```bash
   python main.py
   ```

## 🧠 Алгоритм ранжирования (Code Snippet)

Логика адаптации находится в `src/services/adapter.py`.

$$
Score = W_{base} + (W_{usage\_factor} \times \ln(1 + N_{clicks}))
$$

Это гарантирует, что кнопка "Пить" (Приоритет 100) будет выше кнопки "Игрушка" (Приоритет 20), даже если "Игрушку" выбирали 50 раз, а "Пить" — 0.
